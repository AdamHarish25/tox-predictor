# Option 1: Use --legacy-peer-deps to bypass the peer dependency check
npm install --legacy-peer-deps

# Option 2: Downgrade date-fns to a compatible version
# npm uninstall date-fns
# npm install date-fns@3.0.0

# Option 3: Install with force flag (use with caution)
# npm install --force
